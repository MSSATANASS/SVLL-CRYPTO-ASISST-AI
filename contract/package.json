{
  "name": "sovereign-ai-contract",
  "version": "1.0.0",
  "description": "Smart contract for Sovereign AI NFT collection",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network base-sepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network base-mainnet",
    "verify": "hardhat verify --network base-mainnet"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^7.0.0",
    "hardhat": "^3.2.0",
    "dotenv": "^16.3.1"
  }
}
